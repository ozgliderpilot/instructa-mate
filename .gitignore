# --- Copyrighted GFA source material -------------------------------------
# Do NOT commit the source guides. They are GFA copyright; the project
# handover requires GFA sign-off before any public / open-source release.
corpus/*.pdf
corpus/*.pptx
# Throwaway text extracts produced while inspecting the PDFs
corpus/_extracted*.txt
# Bake-off dumps are full-page verbatim extractions of the GFA PDFs — same
# copyright gate as the source guides; keep the harness, not its output.
bakeoff/out/
# Stage-2 debug dump of chunk records — verbatim GFA text, same gate
chunks.jsonl

# --- Secrets -------------------------------------------------------------
.env
.env.*
*.key

# --- Terraform -----------------------------------------------------------
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars
terraform/crash.log
terraform/*.tfplan

# --- Python --------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# --- Node (future serving layer) -----------------------------------------
node_modules/

# --- OS / editor ---------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
